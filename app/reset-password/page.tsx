"use client";import {useState} from "react";import {useSearchParams} from "next/navigation";import Link from "next/link";
export default function ResetPassword(){const p=useSearchParams();const email=p.get('email')||'';const token=p.get('token')||'';const[password,setPassword]=useState('');const[confirm,setConfirm]=useState('');const[msg,setMsg]=useState('');const[ok,setOk]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();if(password!==confirm){setMsg('Passwords do not match.');return}const r=await fetch('/api/auth/password/reset',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,token,password})});const d=await r.json();setOk(r.ok);setMsg(r.ok?'Your password has been updated.':d.error||'Unable to reset password.');};return <main className="min-h-[70vh] grid place-items-center px-4"><form onSubmit={submit} className="w-full max-w-md rounded-md border border-autox-border bg-autox-panel p-7"><div className="mb-6 text-2xl font-black text-white">AUTO<span className="text-autox-red">X</span></div><h1 className="text-xl font-bold text-white">Choose a new password</h1>{!ok&&<><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-5 w-full rounded-sm border border-autox-border bg-autox-panel3 px-3 py-3 text-sm text-white outline-none focus:border-autox-red" placeholder="New password"/><input required minLength={8} type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} className="mt-3 w-full rounded-sm border border-autox-border bg-autox-panel3 px-3 py-3 text-sm text-white outline-none focus:border-autox-red" placeholder="Confirm password"/><button className="mt-3 w-full bg-autox-red px-4 py-3 text-sm font-bold text-white">Update Password</button></>}{msg&&<p className="mt-4 text-xs text-autox-gray">{msg}</p>}{ok&&<Link href="/login" className="mt-4 block bg-autox-red px-4 py-3 text-center text-sm font-bold text-white">Sign In</Link>}</form></main>}
