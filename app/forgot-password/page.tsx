"use client";import {useState} from "react";import Link from "next/link";
export default function ForgotPassword(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);const r=await fetch('/api/auth/password/request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json();setMsg(d.message||'If an account exists, a reset email has been sent.');setBusy(false)};return <main className="min-h-[70vh] grid place-items-center px-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border border-autox-border bg-autox-panel p-7"><div className="mb-6 text-2xl font-black text-white">AUTO<span className="text-autox-red">X</span></div><h1 className="text-xl font-bold text-white">Reset your password</h1><p className="mt-2 text-sm text-autox-gray">Enter your account email and we’ll send a secure reset link.</p><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-5 w-full rounded-xl border border-autox-border bg-autox-panel3 px-3 py-3 text-sm text-white outline-none focus:border-autox-red" placeholder="Email address"/><button disabled={busy} className="mt-3 w-full bg-autox-red px-4 py-3 text-sm font-bold text-white">{busy?'Sending...':'Send Reset Link'}</button>{msg&&<p className="mt-4 text-xs text-autox-gray">{msg}</p>}<Link href="/login" className="mt-5 block text-center text-xs font-semibold text-autox-red">Back to sign in</Link></form></main>}
