import {redirect} from "next/navigation";import {getCurrentAccount} from "@/lib/auth-guards";import {AdminAccountsManager} from "@/components/admin/AdminAccountsManager";
export default async function AdminAccountsPage(){const u=await getCurrentAccount();if(!u)redirect('/login?callbackUrl=/admin/admins');if(u.role!=="super_admin")redirect('/admin');return <div><div className="mb-6"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-autox-red">Super Admin</p><h1 className="mt-1 text-2xl font-extrabold text-white">Admin Accounts</h1><p className="mt-1 text-sm text-autox-gray">Invite Store Admins securely, enforce MFA and control access from one place.</p></div><AdminAccountsManager/></div>}
