"use client";import{useState}from"react";export function ProfileForm({initial}:{initial:{name:string;email:string;phone:string}}){const[f,setF]=useState(initial);const[msg,setMsg]=useState('');const save=async()=>{const r=await fetch('/api/account/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.name,phone:f.phone})});const d=await r.json();setMsg(r.ok?'Profile updated.':d.error||'Unable to update');};const c='w-full rounded-xl border border-autox-border bg-autox-panel3 px-3 py-2.5 text-sm text-white outline-none focus:border-autox-red';return <div className="rounded-2xl border border-autox-border bg-autox-panel p-5"><div className="grid gap-4 md:grid-cols-2"><label><span className="mb-1 block text-xs text-autox-gray">Full name</span><input className={c} value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label><span className="mb-1 block text-xs text-autox-gray">Phone</span><input className={c} value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></label><label className="md:col-span-2"><span className="mb-1 block text-xs text-autox-gray">Email</span><input className={`${c} opacity-60`} value={f.email} disabled/><span className="mt-1 block text-[10px] text-autox-gray">Email is tied to your login and is intentionally not changed here.</span></label></div>{msg&&<p className="mt-3 text-xs text-autox-gray">{msg}</p>}<button onClick={save} className="mt-5 rounded-xl bg-autox-red px-5 py-2.5 text-xs font-bold text-white">Save Changes</button></div>}
