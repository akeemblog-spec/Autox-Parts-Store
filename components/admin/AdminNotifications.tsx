/* eslint-disable react-hooks/set-state-in-effect */
"use client";import Link from 'next/link';import { Bell } from 'lucide-react';import { useEffect,useRef,useState } from 'react';
type N={id:string;title:string;message:string|null;href:string;readAt:string|null;createdAt:string};
export function AdminNotifications(){const rootRef=useRef<HTMLDivElement>(null);const[items,setItems]=useState<N[]>([]);const[open,setOpen]=useState(false);const load=async()=>{const r=await fetch('/api/admin/notifications',{cache:'no-store'});if(r.ok)setItems((await r.json()).notifications||[])};useEffect(()=>{load();const t=setInterval(load,60000);return()=>clearInterval(t)},[]);useEffect(()=>{if(!open)return;const close=(event:MouseEvent|TouchEvent)=>{const target=event.target as Node|null;if(target&&!rootRef.current?.contains(target))setOpen(false)};const key=(event:KeyboardEvent)=>{if(event.key==='Escape')setOpen(false)};document.addEventListener('mousedown',close);document.addEventListener('touchstart',close);document.addEventListener('keydown',key);return()=>{document.removeEventListener('mousedown',close);document.removeEventListener('touchstart',close);document.removeEventListener('keydown',key)}},[open]);const unread=items.filter(x=>!x.readAt).length;const mark=async(id:string)=>{await fetch('/api/admin/notifications',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});setItems(v=>v.map(x=>x.id===id?{...x,readAt:new Date().toISOString()}:x));};return <div ref={rootRef} className="relative"><button onClick={()=>setOpen(v=>!v)} className="relative grid h-9 w-9 place-items-center rounded border border-autox-border bg-autox-panel text-autox-gray hover:text-white" aria-label="Admin notifications"><Bell size={16}/>{unread>0&&<span className="absolute -right-1.5 -top-1.5 min-w-5 rounded-full bg-autox-red px-1 text-center text-[9px] font-bold leading-5 text-white">{unread>99?'99+':unread}</span>}</button>{open&&<div className="absolute right-0 top-11 z-50 w-[340px] overflow-hidden rounded-md border border-autox-border bg-autox-panel2 shadow-2xl"><div className="border-b border-autox-border px-4 py-3 text-xs font-bold text-white">Notifications</div><div className="max-h-96 overflow-y-auto">{items.length?items.map(n=><Link key={n.id} href={n.href} onClick={()=>{mark(n.id);setOpen(false)}} className={`block border-b border-autox-border/60 px-4 py-3 ${!n.readAt?'bg-autox-red/5':''}`}><div className="flex gap-2"><span className={`mt-1 h-2 w-2 shrink-0 rounded-full ${!n.readAt?'bg-autox-red':'bg-transparent'}`}/><span><b className="block text-xs text-white">{n.title}</b>{n.message&&<span className="mt-1 block text-[11px] text-autox-gray">{n.message}</span>}</span></div></Link>):<p className="p-5 text-xs text-autox-gray">No notifications yet.</p>}</div></div>}</div>}
