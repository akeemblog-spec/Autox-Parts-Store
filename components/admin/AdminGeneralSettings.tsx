"use client";
import { useState } from "react";
import { Save } from "lucide-react";
const input="w-full rounded-sm border border-autox-border bg-autox-panel3 px-3 py-2.5 text-sm text-white outline-none focus:border-autox-red";
const label="mb-1 block text-[10px] font-bold uppercase tracking-wide text-autox-gray";
export function AdminGeneralSettings({initial}:{initial:Record<string,string>}){const[s,setS]=useState(initial);const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);const save=async(data:Record<string,string>)=>{setBusy(true);setMsg('');const r=await fetch('/api/admin/storefront',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'settings',data})});setBusy(false);setMsg(r.ok?'Settings saved.':'Unable to save settings.');};return <><section id="store" className="rounded-md border border-autox-border bg-autox-panel p-5"><div className="mb-4 flex items-center justify-between"><div><h2 className="font-bold text-white">Store Settings</h2><p className="mt-1 text-xs text-autox-gray">Basic contact information used by the store.</p></div><button disabled={busy} onClick={()=>save({store_name:s.store_name||'',support_email:s.support_email||'',support_phone:s.support_phone||'',email_logo_url:s.email_logo_url||''})} className="flex items-center gap-2 rounded-sm bg-autox-red px-4 py-2 text-xs font-bold text-white"><Save size={14}/>Save</button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><label><span className={label}>Store name</span><input className={input} value={s.store_name||'AutoX Parts Store'} onChange={e=>setS({...s,store_name:e.target.value})}/></label><label><span className={label}>Support email</span><input className={input} value={s.support_email||''} onChange={e=>setS({...s,support_email:e.target.value})}/></label><label><span className={label}>Support phone</span><input className={input} value={s.support_phone||''} onChange={e=>setS({...s,support_phone:e.target.value})}/></label><label><span className={label}>Email logo URL</span><input className={input} value={s.email_logo_url||''} onChange={e=>setS({...s,email_logo_url:e.target.value})} placeholder="https://.../autox-logo.png"/><span className="mt-1 block text-[10px] text-autox-gray">Use an absolute public image URL for verification/reset emails.</span></label></div></section><section id="shipping" className="rounded-md border border-autox-border bg-autox-panel p-5"><div className="mb-4 flex items-center justify-between"><div><h2 className="font-bold text-white">Shipping Settings</h2><p className="mt-1 text-xs text-autox-gray">Configure standard delivery and the extra same-day dispatch charge.</p></div><button disabled={busy} onClick={()=>save({delivery_fee:s.delivery_fee||'500',express_delivery_fee:s.express_delivery_fee||'750',express_delivery_enabled:s.express_delivery_enabled||'true'})} className="flex items-center gap-2 rounded-sm bg-autox-red px-4 py-2 text-xs font-bold text-white"><Save size={14}/>Save</button></div><div className="grid gap-4 md:grid-cols-3"><label><span className={label}>Standard delivery fee (LKR)</span><input type="number" min="0" className={input} value={s.delivery_fee||'500'} onChange={e=>setS({...s,delivery_fee:e.target.value})}/></label><label><span className={label}>Express delivery extra fee (LKR)</span><input type="number" min="0" className={input} value={s.express_delivery_fee||'750'} onChange={e=>setS({...s,express_delivery_fee:e.target.value})}/><span className="mt-1 block text-[10px] text-autox-gray">Added on top of standard delivery.</span></label><label><span className={label}>Express delivery</span><select className={input} value={s.express_delivery_enabled||'true'} onChange={e=>setS({...s,express_delivery_enabled:e.target.value})}><option value="true">Enabled</option><option value="false">Disabled</option></select><span className="mt-1 block text-[10px] text-autox-gray">Same-day dispatch option at checkout.</span></label></div></section>{msg&&<p className="text-xs text-autox-gray">{msg}</p>}</>}
